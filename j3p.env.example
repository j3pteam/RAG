# The house brand. Every value here is the code default — this file exists
# so the current deployment is documented in the same form as any other,
# not because it needs setting.
ORG_NAME=J3P Health
ORG_SHORT=J3P
ORG_LEGAL_NAME=Residency Select LLC dba J3P Health
ORG_PRINCIPAL=Alan Friedman
PERSONA_NAME=J3P Advisor
CONTACT_EMAIL=clientservices@j3p.health
ORG_NAMING_RULES=__default__
FLASK_SECRET_KEY=   # set in Railway only — never commit a real value
